import type {ReactNode} from 'react';
import {ProfileSettingsComponent} from '../../../fragments/profile/src/settings';

export default function ProfileSettingsPage(): ReactNode {
  return <ProfileSettingsComponent />;
}
